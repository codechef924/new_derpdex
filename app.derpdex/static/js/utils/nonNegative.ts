/**
 * Given a param of number, returns the value of the parameter that is non-negative as representation
 * @param v to return non-negative
 */
export function nonNegative(v: number) {
  if (v > 0) return v
  else return 0
}
